.block
  .secondary-navigation
    %ul.wat-cf
      %li.first.active=link_to pat(:list), url(:auctions, :index)
      %li=link_to pat(:new), url(:auctions, :new)
  .content
    %h2.title All Auctions
    .inner
      =simple_table(@auctions,[:title,:auction_date,:description],
            :edit =>   "auctions/edit/${row.id}",
            :delete => "auctions/destroy/${row.id}",
            :click =>  "auctions/${row.id}/bidders")
      .actions-bar.wat-cf
        .actions="&nbsp;"