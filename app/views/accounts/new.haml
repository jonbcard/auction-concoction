.block
  .content
    %h2.title
      =pat(:new)
      =mt(:account)
    .inner
      -form_for :account, url(:accounts, :create), :class => :form do |f|
        =partial "accounts/form", :locals => { :f => f }